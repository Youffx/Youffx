name: Lineage CI Release

on:
  workflow_dispatch:
    inputs:
      config_index:
        description: "Select (0,1,2,3,4)"
        required: true
        default: "2"
      do_ksu:
        description: "KernelSU"
        required: false
        type: boolean
        default: true
      do_ksun:
        description: "KernelSU-Next"
        required: false
        type: boolean
        default: false
      do_sukisu:
        description: "SukiSU"
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  actions: read

env:
  CCACHE_DIR: ${{ github.workspace }}/.ccache

jobs:

# =========================
# BUILD SUKISU
# =========================
  build-sukisu:
    name: Build SukiSU
    runs-on: ubuntu-latest
    outputs:
      kern_ver: ${{ steps.kinfo.outputs.version }}
      commit_log: ${{ steps.kinfo.outputs.commit }}
      compiler_ver: ${{ steps.kinfo.outputs.compiler }}
      device_name: ${{ steps.cfg.outputs.name }}
      build_time: ${{ steps.calc.outputs.duration }}
      zip_hash: ${{ steps.calc.outputs.hash }}

    steps:
      - name: Start Timer
        id: start_timer
        run: echo "start=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            bc bison build-essential ccache curl flex git gnupg gperf \
            libelf-dev liblz4-tool libncurses5-dev libssl-dev lzop rsync zip zlib1g-dev jq

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ccache-ksu-${{ runner.os }}-${{ github.event.inputs.config_index }}-${{ github.run_id }}
          restore-keys: |
            ccache-ksu-${{ runner.os }}-${{ github.event.inputs.config_index }}-
            ccache-ksu-${{ runner.os }}-

      - name: Cache Clang 20
        id: cache-llvm
        uses: actions/cache@v4
        with:
          path: llvm
          key: llvm-mlgo-r547379

      - name: Load Config
        id: cfg
        run: |
          CONFIG=$(jq -c ".[$CONFIG_INDEX]" configs/builds.json)
          NAME=$(echo "$CONFIG" | jq -r '.name')
          echo "config=$CONFIG" >> $GITHUB_OUTPUT
          echo "name=$NAME" >> $GITHUB_OUTPUT
        env:
          CONFIG_INDEX: ${{ github.event.inputs.config_index }}

      - name: Clone Kernel
        env:
          PAT_TOKEN: ${{ secrets.REPO_A_PAT }}
        run: |
          REPO=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.repo')
          BRANCH=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.branch')
          DEPTH=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.fetchDepth')

          REPO=$(echo "$REPO" | sed "s|https://|https://${PAT_TOKEN}@|")

          git clone --depth=$DEPTH --branch $BRANCH "$REPO" kernel

      - name: Setup Clang 20
        run: |
          if [ ! -d "llvm/bin" ]; then
            export LLVM_DIR="$PWD/llvm"
            rm -rf "$LLVM_DIR"
            mkdir -p "$LLVM_DIR"
            wget -qO- "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r547379.tar.gz" | tar xz -C "$LLVM_DIR"
            wget -qO- "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/mlgo-models.tar.gz" | tar xz -C "$LLVM_DIR" || true
          fi
          echo "$PWD/llvm/bin" >> $GITHUB_PATH

      - name: Setup ccache
        run: |
          ccache -M 10G
          ccache -o compression=true
          ccache -o base_dir=$GITHUB_WORKSPACE
          ccache -o hash_dir=false
          ccache -z

      - name: Apply Root Method (SukiSU)
        if: github.event.inputs.do_sukisu == 'true'
        run: |
          cd kernel
          curl -LSs https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh | bash -s builtin
          cd ..

      - name: Apply Root Method (KernelSU-Next)
        if: github.event.inputs.do_ksun == 'true'
        run: |
          cd kernel
          curl -LSs https://raw.githubusercontent.com/Youffx/KernelSU-Next/legacy-susfs/kernel/setup.sh | bash -s legacy-susfs
          cd ..

      - name: Build Kernel (SukiSU)
        run: |
          DEFCONFIG=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '."ksu-defconfig"')

          export ARCH=arm64
          export SUBARCH=arm64
          export PATH="$PWD/clang/bin:$PATH"
          export KBUILD_BUILD_USER="youffx"
          export KBUILD_BUILD_HOST="collection"
          export CCACHE_DIR="$GITHUB_WORKSPACE/.ccache"
          export CC="ccache clang"
          export CXX="ccache clang++"
          export CCACHE_COMPILERCHECK=content
          export CCACHE_MAXSIZE=10G

          ccache -s

          cd kernel

          make O=out $DEFCONFIG
          make O=out olddefconfig

          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            CC="ccache clang" \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            Image.gz-dtb

      - name: Package Flashable Zip (KSU)
        run: |
          DATE=$(TZ='Asia/Jakarta' date +"%Y%m%d-%H%M")
          CONF_NAME="${{ github.event.inputs.localversion }}"
          ZIP_NAME="Lineage-23.2-KSU-${DATE}"

          IMAGE="kernel/out/arch/arm64/boot/Image.gz-dtb"
          mkdir -p output
          cp "$IMAGE" .github/Flash/kernels/

          cd .github/Flash/
          zip -r "../../output/${ZIP_NAME}.zip" ./*
          cd ../../

      - name: Calculate Metadata
        id: calc
        run: |
          END=$(date +%s)
          DIFF=$((END - ${{ steps.start_timer.outputs.start }}))
          MIN=$((DIFF / 60))
          SEC=$((DIFF % 60))
          echo "duration=${MIN}m ${SEC}s" >> $GITHUB_OUTPUT
          HASH=$(md5sum output/*.zip | awk '{print $1}')
          echo "hash=$HASH" >> $GITHUB_OUTPUT

      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: zip-ksu
          path: output/*.zip

      - name: Show ccache statistics
        run: ccache -s

      - name: Get Kernel Info
        id: kinfo
        run: |
          cd kernel
          echo "commit=$(git log -1 --format='%h %s')" >> $GITHUB_OUTPUT
          echo "version=$(make kernelversion)" >> $GITHUB_OUTPUT
          echo "compiler=$($GITHUB_WORKSPACE/clang/bin/clang --version | head -n 1 | cut -d'(' -f1)" >> $GITHUB_OUTPUT
          

# =========================
# BUILD NONKSU
# =========================
  build-nonksu:
    name: Build NONKSU
    runs-on: ubuntu-latest
    outputs:
      build_time: ${{ steps.calc.outputs.duration }}
      zip_hash: ${{ steps.calc.outputs.hash }}

    steps:
      - name: Start Timer
        id: start_timer
        run: echo "start=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            bc bison build-essential ccache curl flex git gnupg gperf \
            libelf-dev liblz4-tool libncurses5-dev libssl-dev lzop rsync zip zlib1g-dev jq

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ccache-nonksu-${{ runner.os }}-${{ github.event.inputs.config_index }}-${{ github.run_id }}
          restore-keys: |
            ccache-nonksu-${{ runner.os }}-${{ github.event.inputs.config_index }}-
            ccache-nonksu-${{ runner.os }}-

      - name: Cache Clang 20
        id: cache-llvm
        uses: actions/cache@v4
        with:
          path: llvm
          key: llvm-mlgo-r547379

      - name: Load Config
        id: cfg
        run: |
          CONFIG=$(jq -c ".[$CONFIG_INDEX]" configs/builds.json)
          NAME=$(echo "$CONFIG" | jq -r '.name')
          echo "config=$CONFIG" >> $GITHUB_OUTPUT
          echo "name=$NAME" >> $GITHUB_OUTPUT
        env:
          CONFIG_INDEX: ${{ github.event.inputs.config_index }}

      - name: Clone Kernel
        env:
          PAT_TOKEN: ${{ secrets.REPO_A_PAT }}
        run: |
          REPO=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.repo')
          BRANCH=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.branch')
          DEPTH=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '.kernel.fetchDepth')

          REPO=$(echo "$REPO" | sed "s|https://|https://${PAT_TOKEN}@|")

          git clone --depth=$DEPTH --branch $BRANCH "$REPO" kernel

      - name: Setup Clang 20
        run: |
          if [ ! -d "llvm/bin" ]; then
            export LLVM_DIR="$PWD/llvm"
            rm -rf "$LLVM_DIR"
            mkdir -p "$LLVM_DIR"
            wget -qO- "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r547379.tar.gz" | tar xz -C "$LLVM_DIR"
            wget -qO- "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/mlgo-models.tar.gz" | tar xz -C "$LLVM_DIR" || true
          fi
          echo "$PWD/llvm/bin" >> $GITHUB_PATH

      - name: Setup ccache
        run: |
          ccache -M 10G
          ccache -o compression=true
          ccache -o base_dir=$GITHUB_WORKSPACE
          ccache -o hash_dir=false
          ccache -z

      - name: Build Kernel (NONKSU)
        run: |
          DEFCONFIG=$(echo '${{ steps.cfg.outputs.config }}' | jq -r '."nonksu-defconfig"')

          export ARCH=arm64
          export SUBARCH=arm64
          export PATH="$PWD/clang/bin:$PATH"
          export KBUILD_BUILD_USER="youffx"
          export KBUILD_BUILD_HOST="collection"
          export CCACHE_DIR="$GITHUB_WORKSPACE/.ccache"
          export CC="ccache clang"
          export CXX="ccache clang++"
          export CCACHE_COMPILERCHECK=content
          export CCACHE_MAXSIZE=10G

          ccache -s

          cd kernel

          make O=out $DEFCONFIG
          make O=out olddefconfig

          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            CC="ccache clang" \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            Image.gz-dtb

      - name: Package Flashable Zip (NONKSU)
        run: |
          DATE=$(TZ='Asia/Jakarta' date +"%Y%m%d-%H%M")
          CONF_NAME="${{ github.event.inputs.localversion }}"
          ZIP_NAME="Lineage-23.2-NONKSU-${DATE}"

          IMAGE="kernel/out/arch/arm64/boot/Image.gz-dtb"
          mkdir -p output
          cp "$IMAGE" .github/Flash/kernels/

          cd .github/Flash/
          zip -r "../../output/${ZIP_NAME}.zip" ./*
          cd ../../

      - name: Calculate Metadata
        id: calc
        run: |
          END=$(date +%s)
          DIFF=$((END - ${{ steps.start_timer.outputs.start }}))
          MIN=$((DIFF / 60))
          SEC=$((DIFF % 60))
          echo "duration=${MIN}m ${SEC}s" >> $GITHUB_OUTPUT
          HASH=$(md5sum output/*.zip | awk '{print $1}')
          echo "hash=$HASH" >> $GITHUB_OUTPUT

      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: zip-nonksu
          path: output/*.zip

      - name: Show ccache statistics
        run: ccache -s

      - name: Get Kernel Info
        id: kinfo
        run: |
          cd kernel
          echo "commit=$(git log -1 --format='%h %s')" >> $GITHUB_OUTPUT
          echo "version=$(make kernelversion)" >> $GITHUB_OUTPUT
          echo "compiler=$($GITHUB_WORKSPACE/clang/bin/clang --version | head -n 1 | cut -d'(' -f1)" >> $GITHUB_OUTPUT
          

# =========================
# RELEASE
# =========================
  release:
    name: Release
    needs: [build-sukisu, build-nonksu]
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Collect ZIPs
        run: |
          mkdir final
          find . -name "*.zip" -exec cp {} final/ \;

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: Release-${{ github.run_id }}
          name: Kernel Release
          body: |
            ### Lineage Kernel Release

            **Build Details:**
            - Latest SukiSU
            - Support EROFS
            - Support both AOSP & NON-AOSP
            - Sync with latest Lineage 23.2 kernel source

            **Includes:**
            - SukiSU Kernel
            - NON-KSU Kernel
          files: final/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notification
        env:
          TOKEN: ${{ secrets.TG_TOKEN }}
          CHATID: ${{ secrets.TG_CHATID }}
        run: |
          tg() {
            curl -sX POST https://api.telegram.org/bot"${TOKEN}"/sendMessage \
              -d chat_id="${CHATID}" \
              -d parse_mode=Markdown \
              -d disable_web_page_preview=true \
              -d text="$1"
          }

          BUILD_DATE=$(date +"%a %b %d %H:%M:%S %Z %Y")

          MESSAGE="*Lineage CI Release*
          *Kernel Version:* \`${{ needs.build-sukisu.outputs.kern_ver }}\`
          *Compiler:* \`${{ needs.build-sukisu.outputs.compiler_ver }}\`
          *Date:* \`$BUILD_DATE\`
          *Build Variants:* \`SukiSU, Non-KSU\`
          *Latest Commit:* \`${{ needs.build-sukisu.outputs.commit_log }}\`
          *Build Statistics:*
          • *SukiSU:* *${{ needs.build-sukisu.outputs.build_time }}* | *sha256:* \`${{ needs.build-sukisu.outputs.zip_hash }}\`
          • *Non-KSU:* *${{ needs.build-nonksu.outputs.build_time }}* | *sha256:* \`${{ needs.build-nonksu.outputs.zip_hash }}\`

          [Check Release](https://github.com/${{ github.repository }}/releases/tag/Release-${{ github.run_id }})"

          tg "$MESSAGE"
