name: Cherry Pick

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Target branch (e.g. dev OR commitSHA newBranch)"
        required: true
      source:
        description: "Source repo (format: user/repo)"
        required: true
      commits:
        description: "Commit IDs (space separated)"
        required: true

jobs:
  cherry-pick:
    runs-on: ubuntu-latest

    steps:
      - name: Restore Repo Cache
        uses: actions/cache@v4
        with:
          path: repo
          key: repo-${{ github.event.inputs.branch }}

      - name: Prepare Repository
        run: |
          INPUT="${{ github.event.inputs.branch }}"
          REPO_URL="https://github.com/Youffx/kernel_xiaomi_rosemary.git"

          PARTS=($INPUT)

          if [ ${#PARTS[@]} -eq 2 ]; then
            TARGET="${PARTS[0]}"
            CUSTOM_BRANCH="${PARTS[1]}"
          else
            TARGET="$INPUT"
            CUSTOM_BRANCH=""
          fi

          echo "TARGET=$TARGET"
          echo "CUSTOM_BRANCH=$CUSTOM_BRANCH"

          if [ -d "repo/.git" ]; then
            echo "Using cached repository..."
            cd repo
            git remote set-url origin $REPO_URL
            git fetch origin --prune
          else
            echo "Cloning fresh repository..."
            git clone $REPO_URL repo
            cd repo
          fi

          git fetch origin --tags

          if git ls-remote --heads origin "$TARGET" | grep -q "refs/heads/$TARGET"; then
            echo "Target adalah branch yang sudah ada: $TARGET"
            git checkout -B "$TARGET" "origin/$TARGET"
            git pull origin "$TARGET" --rebase || true
            echo "WORK_BRANCH=$TARGET" >> $GITHUB_ENV

          else
            echo "Target dianggap sebagai commit SHA: $TARGET"
            git fetch origin "$TARGET" || true

            if [ -n "$CUSTOM_BRANCH" ]; then
              NEW_BRANCH="$CUSTOM_BRANCH"
              echo "Menggunakan custom branch: $NEW_BRANCH"
            else
              NEW_BRANCH="cherry-pick-${TARGET:0:7}"
              echo "Menggunakan auto branch: $NEW_BRANCH"
            fi

            git checkout -b "$NEW_BRANCH" "$TARGET"
            echo "WORK_BRANCH=$NEW_BRANCH" >> $GITHUB_ENV
          fi

      - name: Add Source Repository
        run: |
          cd repo
          git remote remove source || true
          git remote add source https://github.com/${{ github.event.inputs.source }}.git
          git fetch source

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Cherry Pick Commits
        run: |
          cd repo

          REJ_COUNT=0

          for commit in ${{ github.event.inputs.commits }}
          do
            echo "Cherry-picking $commit"

            git cherry-pick $commit || true

            COUNT=$(find . -name "*.rej" | wc -l)
            REJ_COUNT=$COUNT

            echo "Current .rej count: $REJ_COUNT"

            git add .
            git commit -m "Cherry-pick $commit (auto)" || true
          done

          echo "FINAL_REJ_COUNT=$REJ_COUNT" >> $GITHUB_ENV

      - name: Check Reject Threshold
        run: |
          echo "Total .rej files: $FINAL_REJ_COUNT"

          if [ "$FINAL_REJ_COUNT" -gt 3 ]; then
            echo "Too many .rej files! Cancel push."
            exit 1
          fi

      - name: Push to Repository
        if: success()
        env:
          PAT_TOKEN: ${{ secrets.REPO_A_PAT }}
        run: |
          cd repo
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/Youffx/kernel_xiaomi_rosemary.git
          git push origin $WORK_BRANCH

      - name: Save Repo Cache
        uses: actions/cache@v4
        with:
          path: repo
          key: repo-${{ github.event.inputs.branch }}
